•What is a gitignore file and what should it include for a Java project?
gitignore file contains ignore rules. It specifically contains rules that are meant to be shared through the repository, so it should be included the hidden files for java project.  
•What does fast-forward in Git mean?
 A fast-forward is what Git does when you merge or rebase against a branch that is simply ahead the one you have checked-out.
•Why should you have different branches?
 to solve the conflicts and multiple developers can work on same time.
•In a project how many remote Git repositories do you usually have?
 each developer in the team can have its own branch so at least no of branch same as developers 